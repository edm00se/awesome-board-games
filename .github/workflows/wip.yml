name: WIP

on:
  pull_request:
    types:
      - opened
      - edited
      - labeled
      - unlabeled
      - synchronize

jobs:
  wip:
    runs-on: ubuntu-latest
    name: Work In Progress Checker
    steps:
      - name: Work In Progress Action
        uses: wow-actions/wip@master
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          labels: 'do-not-merge', 'work in progress', 'wip', 'needs work', '🚧'